import { Request, Response } from "express";
import { rpName } from "../constants";

export const authOptions = async (req: Request, res: Response) => {};

export const authVerify = async (req: Request, res: Response) => {};
